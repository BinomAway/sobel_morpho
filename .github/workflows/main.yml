name: workflow_test1
on: 
  push:
    branches: [master]
  workflow_dispatch:
    branches: [master]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: check repository
      uses: actions/checkout@v2
      
    - name: build and test
      run: |
        cmake CMakeLists.txt
        make
        ./Release/sobel
